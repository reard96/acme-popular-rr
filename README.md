# Acme Popular Users React-Redux

CRUD app built with React and Redux to show and edit users & their ratings.

To start the application, download the directory, then run `npm install` followed by `npm start`.
